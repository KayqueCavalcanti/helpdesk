services:
  api:
    build: ./backend
    ports:
      - "8001:8001"
    volumes:
      - ./backend:/app
      - ./frontend:/frontend
      - db_data:/app/data
    env_file:
      - .env
    restart: unless-stopped

volumes:
  db_data:
